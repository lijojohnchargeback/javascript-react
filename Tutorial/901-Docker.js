/*
   - docker build -t aws-docker-tutorial .
   docker run -it -p 9000:3010


   git 
     git is collaboration tool which handles a team project
      - git add .
      - git commit -m name
      - git push 
      - git restore  --stagged fileName.ext = remove files from stagging
      - git log = it will give all history of commit made in the project
      - git reset commitId = It will get all commit till this commit id


 */
