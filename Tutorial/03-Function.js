/*
 function are subprogram which are used to compute a value or a perform a task
3A)
    A    = build in function  = alert() valuefo
        user defined funciotn
        function function_name (){
        block statement
        }
        function_name()
        where ever function calls it will display.

                        EG: function india (){
                        console.log("i AM FINE")
                        }
                        function manoram(){
                        console.log("I went to market")
                        }

                        india()
                        manoram()

    B) function with parameter
        function funciton_name(param1,param2...){
        bod of the function
        }
        parameter also known as argument
        function_name(params,parma2)
                EG)  function india (one,two){
                    console.log("i AM FINE" + " " + one + " " +  two)
                    }
                    india("runs")
    function parameter missing
        missing vlau are set to undefined
    C) too many argument
        if function is called too many agrument it will build in object
        using argument object using index number
    argument object
        you access number of argument using arguments.

    D) Return statement
        return statement it will any type of data
        it will stop execution

    E) Function expression
        create a functiona assign to variable is called function expression
        function expresstion is not hoisted
    F) Annonymous function
        An anonymous function is a function that was declared without any named identifier to refer to it. As such, an anonymous function is usually not accessible after its initial creation.
        
        it can be store in store in varirable
        funciton(){

        } this is called call back function
        we can pass parameter 

    G) Argument annomouse function
        function runs (mind){
        return mind
        }
    3H) Call back function - a function pass another function as parameter is knowns as call back funciton
    pws
3B) 
    */
